{
  "name": "tests",
  "version": "1.0.0",
  "scripts": {
    "test": "docker run --rm --link lupus_proxy_1:proxy --link lupus_mongo_1:mongo --network lupus_default -v $(pwd)/..:/app lupus-tests"
  },
  "license": "MIT",
  "dependencies": {
    "apickli": "^1.7.0",
    "cucumber": "^1.2.2",
    "mongoose": "^4.5.10"
  }
}
